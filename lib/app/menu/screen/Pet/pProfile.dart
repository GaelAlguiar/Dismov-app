import 'package:flutter/material.dart';

class SampleWidget extends StatelessWidget {
  String? id1;
  SampleWidget({super.key, this.id1});
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}